Using this as a way to learn Go.

The purpose will be an api that supports an app that tracks daily mood (based on user input), "good" and "bad" things, and eventually a way to correlate things that are "good".
